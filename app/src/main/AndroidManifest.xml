<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.vinitkumaragarwal.orderguru">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.CALL_PHONE" />

    <application
        android:name=".GlobalVariable"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher_logo2"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_logo2"
        android:screenOrientation="portrait"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".WelcomeActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".SignUpActivity" />
        <activity android:name=".loginActivity" />
        <activity android:name=".DashboardActivity" />
        <activity android:name=".client" />
        <activity android:name=".addClientActivity" />
        <activity android:name=".user_info_Activity" />
        <activity android:name=".supplierActivity" />
        <activity android:name=".productmaster" />
        <activity android:name=".productcategoryActivity" />
        <activity android:name=".productsubcategoryActivity" />
        <activity android:name=".ProductsActivity"></activity>
    </application>

</manifest>